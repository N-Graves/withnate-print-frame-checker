{
  "name": "withnate-print-frame-checker",
  "version": "0.1.0",
  "private": true,
  "description": "How big can you print it, which UK frame fits, and how much mount border. Runs entirely in the browser.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "demo": "npm run build && node scripts/serve.mjs"
  },
  "dependencies": {
    "@nasdigitaluk/withnate-tool-core": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "author": "Nathan Graves",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/N-Graves/withnate-print-frame-checker.git"
  },
  "bugs": { "url": "https://github.com/N-Graves/withnate-print-frame-checker/issues" },
  "homepage": "https://github.com/N-Graves/withnate-print-frame-checker#readme",
  "keywords": ["print", "framing", "dpi", "mount", "browser", "client-side", "withnate"]
}
